fastapi==0.110.0
uvicorn[standard]==0.29.0  # ASGI server
nba_api==1.10.1             # For accessing NBA stats
pandas==2.2.2              # Data manipulation
python-multipart==0.0.9    # Needed if you later handle file uploads
httpx==0.27.0              # Optional, used if you want async HTTP requests
# Optional dependencies for additional features
sqlalchemy==2.0.20        # For database interactions

# Dockerfile.backend
FROM python:3.12-slim

WORKDIR /app

# System deps that help pandas/numpy and requests perform better
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential curl ca-certificates && \
    rm -rf /var/lib/apt/lists/*

# Install Python deps
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy your code
COPY . .

# Uvicorn server
ENV HOST=0.0.0.0
ENV PORT=8000

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
